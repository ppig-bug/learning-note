# Q-learning算法概念

    目标：学习到最优的action value函数Q*(s,a)
            本质上是要学习到一个行为s,列为a的表格；我们初始化一个Q表，用它来存储最优Q值的近似，不断更新
            更新公式：(α代表学习率)
$$Q(s,a) \leftarrow Q(s,a) + \alpha \big(R(s,a) + \gamma \max_{a'} Q(s',a') - Q(s,a)\big)$$

Q-learning的步骤：
- 初始化Q表 初始化全为0
    >一开始智能体完全没有经验，对所有动作的好坏一无所知
- 以ε的概率探索 从动作空间里面随机选择一个动作
    >探索的目的：给更好的路线一个被发现的机会
- 以1-ε的概率利用 选择当前状态下Q值最大的动作
    >直接选当下状态Q值最大动作

```
ε = 0.1
rand_num = random(0, 1)
if rand_num < ε:
    a = 随机从动作集合挑选一个动作
else:
    a = argmax_a Q(s,a)   # 选当前状态Q最大的动作
```
- 不断执行动作 获得环境反馈：下一个状态s 即时奖励r
- 利用更新公式来更新表格
- 直到下一个状态达到终止条件，开始下一个episode
- 直到Q表收敛，整个算法终止



**大白话总结：我们边走边即时更新 Q 值（时序差分），这套更新规则数学上保证不断靠近标准答案（压缩映射收敛到不动点）；
再配上 ε-greedy 防止钻牛角尖困在次优路线，反复训练后，就能学到环境里收益最高的行动策略。**


**标准Q-learning代码逻辑：
1.初始化：创建环境，生成Q表，设置超参数
2.训练循环：每局用 ε-greedy 选动作--执行动作--用贝尔曼公式更新表--直到本局结束--衰减探索率，记录本局奖励
3.输出结果：打印最终学到的Q表**

    贝尔曼公式更新逻辑(按照公式)：
    1.best_next_q=np.max(Q_tables[next_state]) if not terminated else 0 
    取下一个状态所有动作最大的Q值，如果已经到达终点或者掉坑，说明没有下一步了，未来价值按0处理
    2.td_target=reward+gamma*best_next_q
    TD目标=即时奖励+折扣后的未来价值(贝尔曼方程右侧)
    3.td_error=td_target-Q_table[state,action]
    TD误差=目标值-当前估计值
    4.Q_tables[state,action]+=alpha*td_error
    更新，让当前Q值朝着TD目标靠近一步
    5.state=next_state


# DQN算法概念
> 全称 Deep Q-Network,深度Q网络；用神经网络代替Q表来逼近Q值，解决状态空间过大的问题（不记表格，改成学习规律）

**DQN额外增加的两个保险**：

1.经验回放（replay Buffer）：

Q-learning是走一步记一步用完就扔；DQN把每一步的状态，动作，奖励，下一个状态存进一个记忆库

2.目标网络（Target Network）

算贝尔曼目标值时，如果用的网络每一步都在变，目标值也会跟着抖动。DQN的做法：复制一份"安静"的网络专门算目标值，主网络慢慢学，每隔N步才把参数同步过去


## Q-learning 与 DQN 对比

| 对比项 | Q-learning | DQN |
|--------|-----------|-----|
| 存知识的地方 | Q 表（表格） | 神经网络（参数） |
| 更新方式 | 直接改表格格子 | 梯度下降调网络参数 |
| 核心公式 | 贝尔曼公式 | 同一个公式 |
| ε-greedy 选动作 | ✅ | ✅ |
| 新增机制 | — | 经验回放 + 目标网络 |
| 适用场景 | 状态空间小（如 FrozenLake 16 格） | 状态空间大（如图像输入） |
| 能力 | 只能回答见过的状态 | 对未见过的状态也能估计（泛化） |



