# 神经网络前向传播核心知识点（ReLU网络视角）
## 1. 符号定义
- $l$：**线性变换**，数学形式 $z = Wx + b$（权重×输入 + 偏置）
- $r$：**ReLU激活函数（非线性操作）**

$$r(z)=\max(0,z)=
\begin{cases}
z & z>0 \\
0 & z\le0
\end{cases}$$

## 2. 基础运算单元
基础流程：
$\boldsymbol{x \xrightarrow{线性l} \xrightarrow{非线性r} \xrightarrow{线性l} \rightarrow \hat{y}}$
1. 线性变换：
$z = Ax+B$
2. 非线性激活：
$a=r(z)$
3. 再次线性变换得到输出

> 重要结论：单纯多层线性变换等价于单层线性变换，**必须插入非线性激活函数，网络才能拟合复杂曲线**。

## 3. 浅层网络结构（图左侧单隐藏层网络）
结构：输入$x$ → 3路并行神经元（线性→ReLU→线性）→ 求和得到预测

$$\hat{y} = l_1\big(r(l_{11}(x))\big)+l_2\big(r(l_{21}(x))\big)+l_3\big(r(l_{31}(x))\big)$$

## 4. ReLU核心性质：可作为分段折线基函数
形如 
$r(ax+b)$
- $a$：控制ReLU斜线部分斜率，正负实现上下翻转
- $b$：控制拐点在横轴上左右平移

示例叠加：
$\hat{y}=r(x)-2r(x-1)+2r(x-2)$
多个平移、缩放、翻转后的ReLU相加，可以拼接成任意形状的分段折线。
> 通俗万能逼近定理：大量ReLU相互叠加，神经网络可以逼近任意连续函数。

## 5. 前向传播定义
数据从输入层向前流动，逐层执行「线性运算→激活函数」，最终计算出预测值$\hat{y}$的完整计算流程。

## 6. 深层网络拓展
多层重复结构：
$x\Rightarrow l\Rightarrow r\Rightarrow l\Rightarrow r\Rightarrow l\Rightarrow \hat{y}$
