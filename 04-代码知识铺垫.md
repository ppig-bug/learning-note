# 张量、API、nn 以及其他知识铺垫

> 核心比喻：**张量 = 装载数据的盒子；nn = 存放神经网络积木的工具箱；API = 工具使用说明书里的调用指令**。训练神经网络，就是把盒子里的数据送入积木搭建的流水线，按照说明书执行运算，不断优化结果。

## 一、张量（Tensor）：存放数字的盒子
张量是 PyTorch 承载数据的基础容器，本质是多维数组，可以看作矩阵的延伸。
按维度直观区分：
- 0维张量：单个数字（标量），例如 `8`
- 1维张量：一串数字（向量），例如 `[2,4,6]`
- 2维张量：多行多列表格（矩阵）
- 3维张量：一叠表格；4维及以上继续向外拓展维度

- `shape`（形状）：盒子的尺寸。比如 `torch.rand(2000, 2)`，形状 `(2000, 2)`，代表2000组数据，每组包含2个数值，刚好对应2000组平面坐标(x,y)。
- 为什么训练必须用张量：深度学习批量处理海量样本，GPU擅长批量矩阵运算；张量规整存放数据，才能充分发挥硬件加速能力。

## 二、API：封装好的功能，标准化调用方式
API（应用程序编程接口），通俗来讲：开发者把复杂功能封装完毕，对外留出统一的“操作按钮”，使用者直接调用，不用关心底层实现。

类比点外卖：你不需要掌握烹饪，只需要照着菜单下单；
代码同理：`nn.Linear(2, 16)` 等价于“创建一个将2维数据映射为16维的全连接层”，内部矩阵运算、参数存储全部封装隐藏。

调用任何API抓住三个核心：**功能是什么、需要传入哪些参数、最终返回什么数据**。
PyTorch常用API分为几大类：
`torch.xxx`（创建张量、数学运算）、`nn.xxx`（网络组件）、`optim.xxx`（参数优化器）、`torch.utils.data`（数据集加载工具）。

## 三、nn（torch.nn）：神经网络积木工具箱
`nn` 是 `torch.nn` 的简写，内置搭建网络所需的全部现成组件：
1. **网络层（基础积木）**
`nn.Linear`全连接层、`nn.ReLU`激活函数、`nn.Conv2d`卷积层（图像处理专用）
2. **容器（组装积木）**
`nn.Sequential`：按先后顺序把层拼接成一条流水线
`nn.Module`：所有网络组件的通用父模板
3. **损失函数（评判标准）**
`nn.CrossEntropyLoss` 分类任务损失、`nn.MSELoss` 回归任务损失
4. **nn.Module**
所有网络零件的公共模板；只需要做两件事情：__init__(列出零件清单)，forward(前向传播的公式)

两种代码对比：
（1）
```python
model=nn.Sequential(
    nn.Linear(2,16),
    nn.ReLU()
    nn.Linear(16,2)
)

（2）
```python
class MLP(nn.Module):
    def __init__(self):
        super().__init_()
        self.fc1=nn.Linear(2,16)
        self.relu=nn.ReLU()
        self.fc2=nn.Linear(16,2)
    def forward(self,x):
        x=self.fc1(x)
        x=self.relu(x)
        return self.fc2(x)
model=MLP()
```



## "循环训练"
训练代码的骨架：


```python
out=model(x)  数据穿过网络得到一个预测
loss=criterion(out,y) 计算与标准答案相比差多少损失
optimizer.zero_grad 把上一轮的梯度清空
loss.backward() 算出每个参数应该怎么改
optimizer.step() 更新参数
```


## 各类优化器的优缺点
- SGD(Stochastic Gradient Descent随机梯度下降)
    每个样本更新一步，简单直观，但是收敛太慢；lr常用0.01起步，训练后期需要手动降学习率才能收敛好
- SGD+Momentum(带惯性)
    比SGD快不少，缺点是动量值也要调
- RMSprop(自适应步长)
    每个参数自己算步长，对变化大的目标稳
- Adam
    自适应步长+动量，学习率默认0.001直接用，缺点就是泛化差一点
- AdamW
    大模型，Transformer专用，缺点也是泛化较差

## 分类和回归

### 一、TP/FP/FN/TN 怎么区分

**字母规律**：
- 第二个字母 = 预测结果：P 预测为正，N 预测为负
- 第一个字母 = 预测对不对：T = 说对了，F = 说错了

| 写法 | 拆开读 | 意思 |
|------|--------|------|
| True Positive (TP) | 预测正 + 对 | 真阳性（蒙对了） |
| False Positive (FP) | 预测正 + 错 | 假阳性（误报/虚惊） |
| False Negative (FN) | 预测负 + 错 | 假阴性（漏报/错过） |
| True Negative (TN) | 预测负 + 对 | 真阴性（蒙对了） |

口诀：先看预测成什么（P/N），再看对不对（T/F）。

### 二、accuracy / precision / recall 怎么区分

**例子**：30 人体检，10 人真患病，模型预测 8 人患病，其中 6 人确实患病
→ TP=6, FP=2, FN=4, TN=18

- **Accuracy** = (6+18)/30 = 80%：对着全体算，整体猜对的比例
- **Precision** = 6/8 = 75%：对着你预测的"正"算，喊"有病"的人里真有病的比例
- **Recall** = 6/10 = 60%：对着实际的"正"算，真有病的人里被找到的比例

**一句话区分**：
- accuracy：我整体蒙对多少
- precision：我喊"是"的人靠谱吗
- recall：真正的"是"我漏了多少
- F1：Precision（精确率）和 Recall（召回率）的**调和平均**：

    F1 = 2 × P × R / (P + R)

    用处：类别不平衡时（正负样本差距大），看 F1 更靠谱；当 Precision 和 Recall 同等重要、不想偏袒任何一方时

###  回归指标总结

回归任务：预测连续数值（房价、温度、位置坐标等）。

#### MSE（均方误差，Mean Squared Error）

MSE = mean( (y - ŷ)² )

- 误差平方的平均，**越小越好**
- 对离群点（异常大误差）敏感

#### MAE（平均绝对误差，Mean Absolute Error）

MAE = mean( |y - ŷ| )

- 绝对误差的平均，**越小越好**
- 不放大极端误差，对离群点更"抗干扰"

#### R²（决定系数，拟合优度）

R² = 1 - 残差平方和 / 总平方和

- 表示模型解释了真实数据多少波动，**越接近 1 越好**
- R² = 1：完美拟合；R² = 0：和"直接猜均值"差不多；R² < 0：比猜均值还差


#### 怎么选

| 情况 | 用哪个 |
|------|--------|
| 一般报告 | MSE + R² |
| 离群点多、不想被带偏 | MAE |
| 想突出"大错不可接受" | MSE（或 RMSE） |




