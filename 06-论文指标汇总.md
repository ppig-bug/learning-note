## Bench-Push 论文指标总结（arXiv:2512.11736）

### 公共符号

- m₀：机器人质量；l₀：机器人实际走的路径长度；l*₀：起点到目标的最短路径长度（只避静态障碍）
- mᵢ：第 i 个可推动物体的质量；lᵢ：该物体被移动的路径长度；l*ᵢ：该物体到目标的最短距离
- K：可推动物体的总数；K'：成功完成子任务（送达/清出）的物体数
- 1_success：是否成功（1/0）；μ：摩擦系数；g：重力

### 导航类指标（Navigation）

**① E_nav（任务效率分，Eq. 1）**
E_nav = 1_success × (l*₀ / l₀)    ∈ [0, 1]

- 衡量：是否到达目标 + 路径是不是最短（又准又省，类似 VLN 的 SPL）
- 好坏：越大越好；= 1 表示"以最短路径到达目标"

**② I_nav（交互代价分，Eq. 2）**
I_nav = (μ·m₀·g·l₀) / (Σᵢ₌₀..K μ·mᵢ·g·lᵢ) = m₀·l₀ / (m₀·l₀ + Σᵢ₌₁..K mᵢ·lᵢ)    ∈ [0, 1]

- 衡量：机器人"纯移动"做功占总做功的比例——推东西越少越好
- 好坏：越大越好；= 1 表示完全没推动任何物体；推重物/推很远会显著拉低分数

### 操作类指标（Manipulation）

**③ S_manip（任务成功分，Eq. 3）**
S_manip = K' / K    ∈ [0, 1]

- 衡量：完成了多少子任务（比如 4 个箱子送达 2 个 → 0.5），支持"部分完成"评分
- 好坏：越大越好

**④ E_manip（操作效率分，Eq. 4）**
E_manip = L*(O') / l₀

- L*(O')：完成 K' 个子任务所需的（近似）最短路径长度（论文用最小生成树 MST 估计）
- l₀：机器人实际走的路径长度
- 衡量：实际路径相对理想路径有多"绕"
- 好坏：越大越好（注意：因为是 MST 下界，不保证在 [0, 1] 内）

**⑤ I_manip（交互代价分）**
I_manip = (m₀·l₀ + Σᵢ∈O' mᵢ·l*ᵢ) / (m₀·l₀ + Σᵢ₌₁..K mᵢ·lᵢ)

- 分子：完成已成功子任务的"最小必要功"（移动机器人 + 把每个完成物体沿最短路径推到目标）
- 分母：实际总做功（同 I_nav 的分母）
- 衡量：操作类任务里"推得值不值"——操作是任务必需，所以看是否浪费
- 好坏：越大越好；越接近 1 说明做功都用在刀刃上

