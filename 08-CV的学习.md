# 

## 区分Dataset和DataLoader

| 对比项 | Dataset | DataLoader |
| --- | --- | --- |
| 管什么 | 第 i 个样本 + 它的标签 | 每次取一批、打乱、喂数据 |
| 你关心 | 数据从哪来、怎么预处理 | 一批几份、要不要乱序 |
| 使用方式 | 被 DataLoader 包装 | 在 for 循环里被遍历取数 |

## 图像在深度学习里
> 图像在深度学习里面=数字张量
- 彩色图=3个二维矩阵叠起来,"通道 高 宽",写成[C,H,W],一堆图打包成一批,最前面加上批量维:[B,C,H,W]
- 看到shape就可以认出数据:第二维是3就是彩色,是1就是黑白

## matplotlib
> 独立的第三方绘画库

| 代码 | 作用 | 通俗理解 |
| --- | --- | --- |
| `plt.subplots(2, 4, figsize=(10,5))` | 创建一张 2 行 4 列的"画板" | 准备 8 个小格子摆图 |
| `fig, axes = ...` | 拿到画板对象 fig 和格子列表 axes | fig = 整块板，axes = 8 个格子 |
| `ax.imshow(img)` | 在某个格子里显示一张图片 | 
| `plt.show()` | 弹出窗口显示所有内容 | 

**重点**:
PyTorch和matplotlib对一张图片的要求不一样,需要转化一下

| | 图片顺序 | 像素范围 | 谁在用 |
| --- | --- | --- | --- |
| PyTorch | `[C, H, W]`（通道在最前） | `-1~1`（标准化后） | 模型、张量计算 |
| matplotlib | `[H, W, C]`（通道在最后） | `0~1` | 给人看 |

### 为什么必须做两个转换
- **顺序不同**：PyTorch 把颜色通道放最前，matplotlib 要颜色放最后。
- **范围不同**：模型吃标准化后的 `-1~1`，显示器只认 `0~1`。

### 固定套路
```python
img = tensor_img.permute(1, 2, 0)   # ① 通道挪到最后
img = img * 0.5 + 0.5               # ② 反归一化（和 Normalize 的数对应）
